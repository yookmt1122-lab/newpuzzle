# Coding Rules

Never read .env files or secret folders.
Use dummy values for all credentials.

Keep HTML semantic.
Use BEM naming for CSS.
Prefer vanilla JavaScript.
Do not rename existing class names unless asked.
Preserve current design and spacing.